<div>
  <% if photo.image_data? %>
    <%= image_tag photo.image_url(:thumb) %> <br>
  <% end %>image
  <p><%= photo.title %> | <%= link_to 'Edit', edit_photo_path(photo) %> | <%= link_to 'Delete',photo, method: :delete,  data: { confirm: "You sure?" }%> |</p>
</div>